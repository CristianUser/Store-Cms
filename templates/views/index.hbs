{{!< default}}
{{!-- <p><a href="/keystone/signin" style="margin-right: 10px" class="btn btn-lg btn-primary">Sign in</a> to use the Admin UI.</p> --}}
  <!-- Page Content -->
          <div id="carouselExampleIndicators" class="carousel slide mb-4" data-ride="carousel">
          <ol class="carousel-indicators">
            {{#each data.album}}
            <li data-target="#carouselExampleIndicators" data-slide-to="{{@index}}"></li>
            {{/each}}
          </ol>
          <div class="carousel-inner" role="listbox">
            {{#each data.album}}
            {{#if @first}}
            <div class="carousel-item active">
            {{else}}
            <div class="carousel-item">
            {{/if}}
              <img class="d-block img-fluid" src="{{ secure_url }}" alt="Second slide">
            </div>
            {{/each}}
          </div>
          <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
          </a>
          <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>
        </div>
  <div class="container">

    <div class="row">
{{!-- 
      <div class="col-lg-3">

        <img class="logo-app" src="/images/logo-app.jpeg" alt="logo">
        <div class="list-group">
          <a href="#" class="list-group-item">Category 1</a>
          <a href="#" class="list-group-item">Category 2</a>
          <a href="#" class="list-group-item">Category 3</a>
        </div>

      </div> --}}
      <!-- /.col-lg-3 -->

      <div class="col-lg-12">

        <div class="row">

		{{#each data.posts}}
			<div data-ks-editable="{{#if ../user}}{{adminEditableUrl ../../user _id}}{{/if}}" class="col-lg-4 col-md-6 mb-4">
				<div class="card h-100">
              		<a href="{{postUrl key}}">{{#if image}}<img src="{{{cloudinaryUrl image width=700 height=400 crop='fit' }}}" class="card-img-top">{{/if}}</a>
					<div class="card-body">
						<h4 class="card-title">
						<a href="{{postUrl key}}">{{{name}}}</a>
						</h4>
						<h5>${{price}}</h5>
						<p class="card-text"><p>{{{content.brief}}}</p></p>
					</div>
						<div class="card-footer">
						<small class="text-muted">&#9733; &#9733; &#9733; &#9733; &#9734;</small>
					</div>
				</div>
			</div>
		{{/each}}

        </div>
        <!-- /.row -->

      </div>
      <!-- /.col-lg-9 -->

    </div>
    <!-- /.row -->

  </div>
